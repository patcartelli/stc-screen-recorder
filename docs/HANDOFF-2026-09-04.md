# Handoff — 2026-09-04, from the Linux review session

Written to be read cold. `CLAUDE.md` is the map and every durable lesson below is
already in it; this is what moved today, what is genuinely open, and one
constraint the next session needs to know about immediately: **this session has
no `swiftc`** (a Linux container). Every Swift change below was verified by
careful reading, TS-side typecheck/tests where applicable, and CI's macOS
runner — never compiled or run locally. None of the new grant tests have
actually executed on real hardware yet. That is the single biggest gap this
handoff exists to flag.

## Open right now

**PR #73 (STC-305)** — open, `patcartelli/stc-screen-recorder#73`, branch
`claude/stc-recorder-review-kyev8y`, head `4b489fd`. **Conflicted with master
TWICE in one sitting** — both times because a different, parallel agent's
STC-309 work also touches the start of `CaptureSession.stop()`: first #75,
then #76 (a follow-up from the same parallel agent) landed minutes later and
conflicted again, this time producing a genuine duplicate block (git's
auto-merge ran the tap-disable/`CFRunLoopStop` calls twice; caught and fixed
by hand, not left for CI to catch). Both resolutions are pushed. **If this PR
is still open when you pick this up, assume it may conflict a THIRD time** —
that other agent is still active (see below) and keeps landing PRs in the
same few lines. Check `gh pr view 73 --json mergeable` before doing anything
else; a `dirty` state means merge master in again before touching CI or
asking for a merge.

**CI was in progress on the latest push (`4b489fd`) as of this writing** —
check `gh pr checks 73` or the Actions tab. If green and mergeable: this PR
is ready for **you, the human, to say "merge"** — per this session's own
instructions, merges only happen on the user's explicit word, never
autonomously.

**What #73 actually contains, briefly:** the original STC-305 fix (a `stop`
racing `start`'s success used to silently strand the request and leak a
running capture), PLUS two more serious bugs a code-review pass caught before
merge — a `CaptureSession.stop()` re-entry race that could call
`AVAssetWriter.finishWriting()` twice (the exact STC-254 crash class,
reachable again through the fix's own new call site), and a cross-session
identity gap where a stale session's stray success could tear down a
*different*, currently-live recording. Both are fixed in the same PR. Details
and the exact failure scenarios are in the PR's own comments — read those
before touching this code again, they're more precise than a summary here.

**The grant tests in this PR (and in the three merged before it) have never
actually run.** `helper/test/stop-during-start.grant.test.ts`,
`helper/test/shutdown-during-recording.grant.test.ts`, and the anchors/camera
changes in #71 are all reasoned-through and CI-compiled, not hardware-verified.
**First thing to do on the Mac: `npm run test:capture`.** If
`stop-during-start.grant.test.ts` ever reports a crash (`h.crash` non-null),
that means the re-entry guard has a real gap — it is watching for exactly the
failure class the review found, on real timing this session could not
produce.

## What merged today

Four PRs, one per ticket, each verified by typecheck + the Linux-safe unit
suite + CI's macOS runner, each merged only after the user said "merge":

- **#69 STC-308** — transform version stamp (`TRANSFORM_VERSION`,
  `transformFingerprint()`) in `project-3` and every export manifest.
- **#70 STC-307** — `recording.json`: a take's media as a segments array with
  stable ids. Schema + loader + fixtures + synthesis from `anchors.json` only
  — **not wired into `loadSession`/`takes.ts`/the renderer**, since nothing
  downstream (re-take, N-minute segmentation) exists yet to consume it.
- **#71 STC-303** — a display-only take no longer claims its camera "recorded
  no frames"; `anchors.camera` is now omitted entirely when no camera was
  requested, present:false only when one was asked for and got nothing.
- **#72 STC-304** — helper `shutdown` now waits for its own `stop` to finish
  (with a backstop and idempotency against overlapping signals) before
  exiting, instead of exiting out from under it.

Master is at `14c4e41` as of this write-up (already includes STC-309 `#75`
and `#76`, the parallel agent's work — not mine, and possibly stale by the
time you read this; see below).

## Filed, not started

**STC-310** — `start()`'s `.failure` branch has the identical
unconditional-state-clobber problem STC-305 fixed in the sibling `.success`
branch, just via a different trigger (a stale `.failure`, e.g. from the 15 s
`startTimeoutSeconds` backstop, landing after a *different* session has since
started and is actively recording). Pre-existing, not introduced by #73, out
of that ticket's stated scope — filed separately rather than folded in.
Backlog, unassigned to any PR.

**STC-306** — a display stream that dies mid-take (`didStopWithError` after a
successful start) leaves the helper stuck saying `recording` forever, frames
or not. Well-specified in Linear, same shape as STC-303-305 (small Swift fix,
clear acceptance criteria, needs `anchors-2`'s `stop.reason` enum to grow a
`stream-stopped` value). This is the next clean pickup if continuing this
session's pattern — genuinely untouched, no PR, no other agent on it as of
this write-up.

## Do not duplicate — another agent is active on this repo right now

A different session/agent is actively landing PRs against `Capture.swift`
while this handoff was being written — **#75 and #76 (both STC-309
follow-ups) merged within about 20 minutes of each other**, each one
conflicting with PR #73 above. As of this write-up:

- **#74 STC-289** — still open, still capture in the helper (branch
  `claude/stc-289-kucfai`). Don't start work on STC-289 — someone is already
  driving it.
- STC-309's own follow-up work (#75, #76) is merged; there may be MORE
  coming from the same agent — it was still active as this was written.

Don't re-touch the tap-disable/`stoppingBegan` path in `CaptureSession.stop()`
without first checking `gh pr list` and `git log --oneline -5 -- helper/src/Capture.swift`
— that exact area has changed twice in the last hour from outside this
session. This file will go stale fast; treat every claim above about "what's
open" as a snapshot, not current truth, and re-verify before acting on it.

## Still blocked on the user, not on engineering

- **STC-302** (public repo) — needs a licence choice and a demo clip filmed
  on the Mac. Can prep the licence file/README polish without those; can't
  finish it.
- **STC-247** (multi-display capture) — needs a second physical display.

## Verification commands

```
npm run typecheck        # THREE tsc passes; a bare `tsc` runs one
npm test                 # everything not needing swiftc or a grant
npm run test:capture     # NEEDS THE MAC — the untested grant tests are here
npm run gate / gate:seek / gate:export / gate:identity
gh pr checks 73           # or the Actions tab — confirm CI before merging
```

## Working on Linux without `swiftc`

No committed config does this — `vitest.config.ts`'s `globalSetup` calls
`helper/build.sh`, which needs a Swift toolchain this container does not
have. The workaround used all session is a small, deliberately **uncommitted**
scratch file at the repo root:

```ts
// vitest.linux.config.ts
import { defineConfig } from "vitest/config";
const ROOT = "/home/user/stc-screen-recorder/";
const resolve = { alias: { "@transform": ROOT + "transform/src" } };
const EXCLUDE = ["**/node_modules/**", "**/*.grant.test.ts", "**/*.slow.test.ts",
  ROOT + "helper/test/**", ROOT + "app/test/helper-client.test.ts", ROOT + "app/test/supervisor.test.ts"];
export default defineConfig({
  test: {
    root: ROOT, exclude: EXCLUDE,
    projects: [
      { resolve, test: { name: "unit", root: ROOT,
        include: ["transform/test/**/*.test.ts", "app/test/**/*.test.ts"],
        exclude: [...EXCLUDE, "**/*.e2e.test.ts"], testTimeout: 15_000 } },
      { resolve, test: { name: "e2e", root: ROOT,
        include: ["app/test/**/*.e2e.test.ts"], exclude: EXCLUDE,
        fileParallelism: false, testTimeout: 15_000 } },
    ],
  },
});
```
Run with `npx vitest run --config vitest.linux.config.ts --project unit`.
**This file kept disappearing between tool calls in this session** for
reasons never root-caused (not a git operation — `git status` showed it gone
with nothing to explain it). If it's missing, recreate it from the block
above rather than searching for a "lost" file. It excludes `helper/test/**`
wholesale even though a few files there (`stop-bounds.test.ts`,
`anchors.test.ts`'s non-Swift assertions) don't strictly need a compiler —
worth a narrower exclude list if this becomes a recurring need rather than a
one-session workaround.

Some `helper/test/*.ts` files ONLY read Swift source as text (regex, no
compilation) — `stop-bounds.test.ts` is one. Those genuinely can run on
Linux with a standalone one-file vitest config (no `globalSetup`); useful for
mutation-verifying a Swift constant's guard without a Mac, as done for
STC-304's `shutdownBackstopMarginSeconds` this session.

## One thing worth carrying forward

**A green CI check and a plausible-sounding fix are not the same as a correct
one**, especially for concurrency code nobody in this session could compile
or run. The STC-305 PR looked done — ticket's acceptance criteria addressed,
tests written, CI green — and a real review pass (not just re-reading my own
diff) found a genuine crash-class regression the ticket itself never
described, in the exact race window the fix was supposed to close. Before
merging any Swift concurrency change from a session that cannot compile
Swift, get a second, adversarial pass on it — the code-review skill, or
equivalent — rather than trusting a clean CI run alone. It is markedly
cheaper than finding out on the Mac, or worse, in production.

## Attribution note for whoever continues this

Commits and PRs from this session end with this session's own
`Claude-Session` URL and `Co-Authored-By` line. A new session should use
*its own* — don't copy the trailers from existing commits into new ones.
