/**
 * Hand-written, and gate-bounds.test.ts's declaration check exists because a
 * .d.mts is a second file that must be widened with the first.
 */
export declare function classifyDecoderStall(debug: unknown): "machine" | "ours";
